[project]
name = "team-shivam-shivam"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic",
    "email-validator",
    "python-multipart",
    "passlib[bcrypt]",
    "python-jose",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "backend.main:app"
